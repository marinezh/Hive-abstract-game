{
  "name": "codespaces-blank",
  "private": true,
  "version": "0.0.0",
  "type": "module",

  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^7.1.7"
  },
  "scripts": {
  "build": "vite build",
  "deploy": "vite build && rm -rf docs/* && cp -r dist/* docs/ && git add docs && git commit -m 'deploy' && git push"
}
}

